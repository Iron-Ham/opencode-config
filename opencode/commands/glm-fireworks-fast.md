---
description: Run an explicit, bounded GLM 5.2 max experiment on Fireworks Fast.
agent: glm_worker
model: fireworks-ai/accounts/fireworks/routers/glm-5p2-fast
subtask: true
---

Execute this exact developer-supplied task with the experimental GLM 5.2 max worker on Fireworks Fast:

$ARGUMENTS

Only the command and its arguments are supplied as task context. Keep the scope bounded, follow applicable repository instructions, preserve unrelated changes, request approval for shell or broad search access, and report concrete verification. Fireworks Fast carries a list-price premium and is an explicit latency experiment, not a default routing path.
