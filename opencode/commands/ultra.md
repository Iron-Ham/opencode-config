---
description: Run a high-effort, proactively delegated Codex-like workflow.
agent: ultra
---

Execute this request in Ultra mode: $ARGUMENTS

Orient in the repository and load applicable instructions before choosing an approach. Consult the isolated advisor before committing to a consequential plan when the transcript is approved for cross-provider transmission; otherwise report that the advisor gate was skipped.

For work that needs multiple implementation and verification turns, create a durable goal because this command explicitly authorizes one. Proactively delegate independent, bounded research, architecture, security, review, and verification tasks through native subagents. Use parallelism only where tasks do not overlap, with at most four concurrent and eight total subagents. Keep the controller responsible for reconciling all durable implementation.

Reconcile subagent disagreement against source evidence. Preserve unrelated changes. Run proportionate tests, inspect the final diff, obtain an adversarial specialist review, and consult the advisor after verification when cross-provider transmission is approved. Continue until the requested outcome has evidence or a concrete blocker requires user authority. Close any goal only with completion evidence or an explicit blocker.
