---
description: Run a high-effort, proactively delegated OpenCode workflow.
agent: ultra
---

Execute this request in Ultra mode: $ARGUMENTS

Orient in the repository and load applicable instructions before choosing an approach. The external advisor tool is unavailable. If independent review would materially change a consequential decision, state the exact bounded question a developer could pass to `/advise`; do not forward or reconstruct the parent transcript.

Create a durable goal for this Ultra invocation because the command explicitly authorizes one. Do not add a token budget, automatic-turn limit, or elapsed-time limit unless the developer explicitly requested that limit; Ultra is intended to remain active for unattended jobs lasting overnight or multiple days. Proactively delegate independent, bounded research, architecture, security, and review tasks through native subagents. Run deterministic verification in the controller, then give the evidence analyst already-produced artifacts and an exact claim checklist when independent interpretation is useful. Use parallelism only where tasks do not overlap, with an instructional ceiling of four concurrent and eight total subagents. Keep the controller responsible for reconciling all durable implementation.

Reconcile subagent disagreement against source evidence. Preserve unrelated changes. Run proportionate tests and inspect the final diff. Add an independent specialist review only when a concrete consequential risk makes the second context worth its cost. Continue until the requested outcome has evidence or a concrete blocker requires user authority. Close any goal only with completion evidence or an explicit blocker.
