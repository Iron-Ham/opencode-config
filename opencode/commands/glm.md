---
description: Run an explicit, bounded GLM 5.2 max experiment on Baseten.
agent: glm_worker
model: baseten/zai-org/GLM-5.2
subtask: true
---

Execute this exact developer-supplied task with the experimental GLM 5.2 max worker on Baseten:

$ARGUMENTS

Only the command and its arguments are supplied as task context. Keep the scope bounded, follow applicable repository instructions, preserve unrelated changes, request approval for shell or broad search access, and report concrete verification. This is an explicit model experiment, not a default routing path.
