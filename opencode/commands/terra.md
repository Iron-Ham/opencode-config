---
description: Run production implementation with the default GPT-5.6 Terra xhigh controller.
agent: terra
---

Execute this request with GPT-5.6 Terra at xhigh effort: $ARGUMENTS

Use this production-default lane for repository-scale iOS work, consequential concurrency or state machines, and tasks whose trust boundaries make recovery expensive. Terra completed both production-shaped confirmations; Luna xhigh crossed the read boundary in both and cost nearly as much overall. Follow all loaded instructions, preserve unrelated changes, verify in proportion to risk, and finish the requested task end to end. Keep delegation bounded. Do not invoke an external advisor tool; if independent review would materially change the decision, state the exact question a developer could pass to `/advise`.
